# GV-SHR-SA-AKI Mortality Prediction

Streamlit online prediction tool based on the final SHAP-simplified CatBoost model.

## Run locally

pip install -r requirements.txt

streamlit run app.py